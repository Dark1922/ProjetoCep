package com.projeto.cep.domain.calcularfrete;

public class CalcularFreteDescontos {
	/*
	public static void CalcularDddIgual(Frete frete ) {
		frete.setDataPrevistaEntrega(LocalDateTime.now().plusDays(1));
		frete.setVlTotalFrete(calcularPrecoTotal(frete, 0.5)); //50% desconto
	}
	public static void CalcularUfIgual(Frete frete ) {
		frete.setDataPrevistaEntrega(LocalDateTime.now().plusDays(3));
		frete.setVlTotalFrete(calcularPrecoTotal(frete, 0.75)); //75% desconto
	}	
	public static void CalcularSemDesconto(Frete frete) {
		frete.setDataPrevistaEntrega(LocalDateTime.now().plusDays(10));
          frete.setVlTotalFrete(calcularPrecoTotalSemDesconto(frete)); //sem desconto
    }	
	  
	public static BigDecimal calcularPrecoTotal(Frete frete, Double desconto) {
	   return frete.getPeso().multiply(new BigDecimal(frete.getPreco() * desconto));
  }
	
	public static BigDecimal calcularPrecoTotalSemDesconto(Frete frete) {
		   return frete.getPeso().multiply(new BigDecimal(frete.getPreco()));
	  }
	
	*/
}

